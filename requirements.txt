# Keras 3 is required to load best_model_v12.h5 (serialised by TF >= 2.16).
tensorflow>=2.16.0
librosa>=0.10.0
soundfile>=0.12.0
scikit-learn>=1.2.0
pandas>=1.5.0
matplotlib>=3.6.0
numpy>=1.23.0
opencv-python>=4.7.0

# Required only for the YAMNet filter in auto_cleanup.py
tensorflow-hub>=0.13.0
# tensorflow-hub imports pkg_resources, which shipped with setuptools until
# setuptools 81 dropped it. Cap below 81 so pkg_resources is still available.
setuptools>=65.0.0,<81
# Used only by scripts/fetch_colobus_library.py to download reference clips.
requests>=2.28.0
# resampy is not required: librosa>=0.10 resamples with soxr by default.
