# Exact pinned versions for reproducibility.
#
# IMPORTANT — Keras 3 / TensorFlow >= 2.16 is REQUIRED.
# The trained checkpoint (best_model_v12.h5) is serialised by Keras 3, whose
# InputLayer stores the `batch_shape` / `optional` fields. Loading it under
# Keras 2 (TensorFlow <= 2.15) fails with:
#   "Unrecognized keyword arguments passed to InputLayer: ['batch_shape', 'optional']"
#
# CONFIRM the exact TensorFlow version on the training machine and match it here:
#   python -c "import tensorflow as tf, keras; print(tf.__version__, keras.__version__)"
# The pin below is a known-good Keras-3 release; change the patch version if the
# training machine reports a different one.
#
# Tested on Python 3.10 / 3.11 (Google Colab, single GPU).
#
# INSTALL: do NOT use `conda install tensorflow` (that channel ships an old
# Keras-2 build and clashes with pip wheels). Create a clean conda env with
# Python only, then `pip install -r requirements-frozen.txt`. See SETUP.md.

tensorflow==2.17.0
librosa==0.10.2
soundfile==0.12.1
scikit-learn==1.4.2
pandas==2.2.2
matplotlib==3.8.0
numpy==1.26.4
opencv-python==4.10.0.84
tensorflow-hub==0.16.1
resampy==0.4.3
