# Versions for the primate detection pipeline. The model (best_model_v12.h5)
# was trained on TF 2.20.0 / Keras 3.13.2 (Colab), but any TF >=2.16 with
# Keras 3 can load it. Ranges are used so the same file works on Windows,
# macOS (Apple Silicon), and Linux — not every TF release is available on
# every platform.
#
# INSTALL: do NOT use `conda install tensorflow` (that channel ships an old
# Keras-2 build and clashes with pip wheels). Create a clean conda env with
# Python only, then `pip install -r requirements-frozen.txt`. See SETUP.md.

tensorflow>=2.16.0
keras>=3.0.0
numpy>=2.0.0,<3
librosa>=0.10.0,<1
soundfile>=0.12.0
scikit-learn>=1.4.0,<2
pandas>=2.1.0,<3
matplotlib>=3.8.0,<4
opencv-python>=4.8.0
tensorflow-hub>=0.16.0

# resampy is NOT required: librosa>=0.10 resamples with soxr (a librosa
# dependency) by default, and the training environment had no resampy
# installed. Uncomment only if you set a resampy-based res_type yourself.
# resampy==0.4.3
